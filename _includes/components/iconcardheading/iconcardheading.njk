<!-- Not in use. Can be deleted.
<div class="col-sm-12 col-lg-4 col-xl-4 card border-0 pr-5">
  <i class="{{ params.icon }}" style="font-size: 3rem;"></i>
  <h3 class="card-title" {% if params.heading_level %} aria-level="{{ params.heading_level }}"{% endif %}>
    {{ params.heading }}
  </h3>
  <p>
    {{ params.body }}
  </p>
</div>
-->