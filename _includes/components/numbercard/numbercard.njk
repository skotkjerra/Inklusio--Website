    <div class="col-sm-12 mr-md-5 col-lg-3  col-xl-3 card border-0">
        <h3>
          <span class="card-title-number">{{ params.primary }}</span>
          <br>
          <span class="card-title-font">{{ params.secondary }}</span>
        </h3>
        <p class="my-4">{{ params.tertiary }}</p>
    </div>