---
layout: layouts/base.njk
templateClass: tmpl-page
---

{% set keywords = translations[page.lang].keywords %}

<div class="container">
	<div class="row">
		<div class="col-12 mb-3">
			{% if title %}<h1>{{ title }}</h1>{% endif %}
			{{ content | safe }}
			<a href="mailto:info@inklusio.dk" class="bg-primary btn-primary p-3 mt-auto rounded">{{ keywords.get_offer }}</a>
		</div>
	</div>
</div>